{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "wasrun --stages --exclude-missing build",
    "dev": "wsrun --exclude-missing dev"
  },
  "devDependencies": {
    "wsrun": "^5.2.1"
  }
}
