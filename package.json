{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "wsrun --stages --exclude-missing build",
    "dev": "wsrun --exclude-missing dev",
    "lint": "eslint .",
    "test": "jest",
    "test:dev": "yarn run test --watch"
  },
  "devDependencies": {
    "jest": "^26.1.0",
    "wsrun": "^5.2.1"
  }
}
