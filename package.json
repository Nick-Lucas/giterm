{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "wsrun --stages --exclude-missing build",
    "dev": "wsrun --exclude-missing dev",
    "lint": "eslint .",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^26.1.0",
    "wsrun": "^5.2.1"
  }
}
