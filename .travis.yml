os: osx
osx_image: xcode9.4
language: node_js
node_js:
  - "8.11.3"
cache: 
  yarn: true
before_install:
  - curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version "1.7.0"
  - export PATH="$HOME/.yarn/bin:$PATH"
script:
  - echo "Starting Build"
  - yarn pack:mac
